	<%= render 'shared/task_form' %>
	<% unless @list.tasks.empty? %>
        <table class="tasks" summary="User tasks">
          <%= render @tasks %>
        </table>
        <%= will_paginate @tasks %>
      <% end %>